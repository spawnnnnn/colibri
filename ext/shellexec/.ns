<?php
    
    if(_DEBUG)
        require_once('shellexec.cls.php');
    
    // insert files into files array in Core object
    Core::$files = array_merge(Core::$files, array(_CORE.'ext/shellexec' => array('shellexec.cls.php')));
?>