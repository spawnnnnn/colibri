<?php
    
    if(_DEBUG)
        require_once('ajax.cls.php');
    
    // insert files into files array in Core object
    Core::$files = array_merge(Core::$files, array(_CORE.'ext/ajax' => array('ajax.cls.php')));
?>